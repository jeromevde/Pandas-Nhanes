{
  "name": "pandas-nhanes-site",
  "version": "1.0.0",
  "description": "NHANES Variables Explorer site",
  "scripts": {
    "build": "node build.js",
    "start": "npm run build && live-server dist --port=8080 --open",
    "dev": "npm run build && live-server dist --port=8080 --open --watch=Examples",
    "serve": "live-server dist --port=8080"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "keywords": ["nhanes", "data", "health", "visualization"],
  "author": "",
  "license": "MIT"
}
