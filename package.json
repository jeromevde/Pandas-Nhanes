{
  "name": "pandas-nhanes-site",
  "version": "1.0.0",
  "description": "NHANES Variables Explorer site",
  "scripts": {
    "start": "live-server . --port=8080 --open",
    "dev": "live-server . --port=8080 --open --watch=Examples",
    "serve": "live-server . --port=8080"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "keywords": ["nhanes", "data", "health", "visualization"],
  "author": "",
  "license": "MIT"
}
